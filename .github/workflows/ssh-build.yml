name: SSH Build

on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        uses: P3TERX/Actions-OpenSSH@master
        env:
          SSH_PASSWORD: twrp123
